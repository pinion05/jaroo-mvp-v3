# Jaroo 교차검증 보고서 (2026-08-31)

**방법**: 지시사항 원문 4종(08:21)·시제품 4종(08:24)·PM 보고서·dev 브랜치 코드를 3개 독립 검증 레인으로 상호 대조.

- **레인 A** — 지시사항 원문 4종 + 시제품 ↔ PM 보고서 (주장의 근거 검증)
- **레인 B** — 전달문서(jaroo_dev_handoff.md) ↔ 현재 코드 (구현 갭 감사)
- **레인 C** — PM 보고서 §4/§5 ↔ 현재 코드 (8/31 기준 재판정)

모든 근거는 파일 직접 독해 기반. 검증 수행: pi 서브에이전트 3개(독립 컨텍스트).

---

## 핵심 결론

1. **PM 보고서는 신뢰 가능** — M1~M4·M7·M8 수치를 시제품에서 전부 재계산 재현했고, 원문 인용(알림 문구·근거 12항목·금지 15개)은 오차 0. §4 현황표도 8/31 코드 재판정에서 **전 행 일치**. 단 표기 3건 정정 필요(아래).
2. **P0 발견 1건 (코드)** — 딥스캔 실패 시 크레딧 선차감 후 **환불 전무** (§6-6 위반). 현재는 결제 미연동이라 무료 통과라 미노출이지만, **과금 활성화 전 반드시 수정**.
3. **원문↔코드의 구조적 갭** — 전달문서의 API 목록·데이터 계약이 실제 구현과 경로/형태가 다름(동등 기능이 다른 경로로). 문서를 개발 명세로 쓰려면 갱신 필요.
4. **문서끼리 모순 2건 추가 확인** — spec v7 §11 "Primary #2B6BE6" vs 전달문서 "액션은 먹색·파랑은 손실 전용", spec v7 §3 데모 타임라인(6~33초) vs 공식 예시 8초/계산값 9초. (색 문제는 이슈 #220 결정으로 사실상 해결됨)

---

## 1. PM 보고서 판정: OK with notes — 정정 3건

| 항목 | 등급 | 내용 |
|---|---|---|
| 여정 ⑤~⑦ "0%" / 지표 4.5/7 | **P1** | 원문(jaroo_overview.md §2) 기준 ⑤대기=딥스캔 진행, ⑥판단=딥스캔 결과, ⑦지속=워치인데, 보고서는 ④⑤⑥⑦을 한 칸씩 밀어 ⑤~⑦을 워치 블록으로 재해석. **결론(워치 미구현)은 §4-3 독립 근거로 유효**하나 단계 매핑 표기는 정정 필요 |
| M5 초록 개수 "3곳" | **P2** | 실제는 **최소 4종 7인스턴스** — plus 태그 4곳(#1A7340), 결론 텍스트, 팀 아바타 배경(#E5F3EB), 차트 음영. 코드 재검증(레인 B)에서 더 광범위 확인: `--jaroo-success` 토큰, 홈 모멘텀 배지(#EAF3DE/#3B6D11), 로딩 화면 positive 태그·완료 카드까지 |
| "시제품 3종 모두 미구현 영역의 목업" | **P2** | 보고서 §3 스스로 "2개는 미구현 화면 목업, 딥스캔은 부분 구현 영역의 완성형"이라 기술 — 내부 불일치 |

그 외: A3의 "점수 폐기 위반" 서술은 원문("UI 폐기, 백엔드 별개")보다 강함(P2). A2·A4·A5·A6 및 "오류 아닌 것 5건"은 전부 원문 대조 타당.

## 2. 코드 갭 감사 (레인 B)

### 머지 전 / 과금 활성화 전 반드시 수정 (blocker)

| 등급 | 항목 | 근거 |
|---|---|---|
| **P0** | **딥스캔 실패 시 크레딧 환불 전무** — `authorizeDeepScanRun`이 `spend_credits` 선차감 후 builder 실패 시 error 응답만 반환. §6-6("차감 금지 + 차감되지 않았어요 명시") 위반. credit_ledger에 refund 사유 존재하나 미사용 | `src/app/api/deepscan/route.ts`, `src/lib/payments/deepscan-entitlement.ts` |
| P1 | 초록색 잔존(실렌더 경로) — deepscan-inline-results.tsx:126,142 / recovery-forecast-card.tsx:21 / loading-screen.module.css:1819,2133,2225 / 홈 모멘텀(jaroo-home-screen.tsx:392) / globals.css `--jaroo-success` | §7 위반 |
| P1 | 이모지 노출 — 홈 🔍(551,555)·🔔(1117), 🎯, 로딩 아바타 🏛️📈🧠(deepscan-loading-types.ts) | §7 위반 |
| P1 | 평단 소수점 노출 경로 — formatCurrencyValue(소수 4자리), formatComputedNumber(4자리), 표시부 averagePrice 원문 출력 | §5-4·§7 위반 |
| P1 | 홈 경로 마이너스 하이픈(`-`) — formatSignedKrw 등. (딥스캔 경로는 U+2212 준수) | §5-4 |

### 다음 스프린트

- §6-2 계좌 병합: 현재 conflict 시 사용자 1건 선택 방식 — 가중평균 합산 + `merged_from` 계좌별 보관 미구현
- §6-4 알림 강도 3단계·안전 하한선, §6-5 감시 배치, `/api/watch` 4종, `/api/deepscan/history`, `DELETE /api/portfolio/{code}` — 미구현 (워치 착수 시 일괄)
- 홈 경로 부호 통일, §2 데이터 계약 정렬(ocr/portfolio/deepscan 응답 필드), 사망 코드 제거(`buildHomeMarketScore`·VKOSPI 문구·`portfolioScoreBreakdown`·"OCR에서 읽어…"·"TODAY'S FORECAST"·"위원 N/M 응답" 용어)

### 참고 — 전달문서 ↔ 코드의 계약 차이 (문서 갱신 권고)

| 문서 | 실제 구현 |
|---|---|
| POST /api/ocr/parse (멀티파트 5장, scanned_count) | POST /api/ocr (JSON 1장, 클라이언트 반복) + POST /api/instruments/resolve (후보, similarity→score) |
| POST /api/portfolio/apply, DELETE /{code} | POST /api/portfolio (전체 동기화 RPC). 개별 삭제 API 없음 |
| GET /api/deepscan/{code} instant→llm | GET /api/deepscan?code= (단일 payload) + committee-status 폴링 2.5초 |
| POST /api/auth/email·google | POST /api/auth/login + Supabase OAuth 플로우 |
| (미문서화) | quotes/current, instruments/resolve, recovery-forecast, deepscan/slim·team-summary·committee-status·perf-summary·briefing-snapshot, telegram/link·webhook, payments/me, market/fx·us-indicators, account/delete |

판정 5단계(강세/우세/관찰/주의/위험) 대신 코드는 4단계(강세/중립⁺/중립/주의), 막대 4칸 — A3 지적과 일치 (이사님 결정 필요).

## 3. 8/31 재판정 (레인 C)

§4-1 여정 ①~⑦ 전 행·§4-3 알림 갱신 문구가 현재 코드와 **전부 일치**로 확인됨. (⑦ 각주의 "채널 인프라 완성·상시 감시 없음", "토글 3개 장식" 등 전부 코드로 재확인)

## 4. 권고 액션 (우선순위)

1. **P0 — 딥스캔 실패 크레딧 환불 구현** (과금 연결 전 필수, §6-6)
2. **A4·A6 스프린트** — 초록 제거·이모지 제거·평단 정수화·부호 통일 (M5 정정 범위 포함, 사망 코드 동시 제거)
3. **전달문서 갱신** — §3 API 목록·§2 계약을 실구현 기준으로 갱신 (이사님 문서라 승인 후)
4. **PM 보고서 표기 3건 정정** — 본 검증으로 확정, 보고서 업데이트 카드에 반영 (완료: 이 PR)
5. A2·A3·A5 결정요청 진행 (기존 계획 유지)
